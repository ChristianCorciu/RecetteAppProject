<div class="container">
    <!-- Titre de la page -->
    <h1 class="page-title">Voici toutes les recettes</h1>
    <div class="search-bar d-flex justify-content-end mb-4">
    <form method="get" action="{{ path('recipe_search') }}" class="form-inline">
        <input type="text" name="q" class="form-control mr-2" placeholder="Rechercher une recette..." />
        <button type="submit" class="btn btn-primary">Rechercher</button>
    </form>
</div>
<!-- Bouton Home en haut à gauche -->
<div class="home-button mb-3">
    <a href="{{ path('app_home') }}" class="btn btn-outline-primary">Accueil</a>
</div>



    <!-- Wrapper row pour organiser les cartes en colonnes -->
    <div class="row">
        {% for recipe in recipes %}
            <div class="col-md-3 mb-4"> <!-- Augmenter la largeur des cartes pour plus de lisibilité -->
                <div class="card recipe-card">
                    <div class="card-body">
                        <h5 class="card-title">{{ recipe.name }}</h5>

                        {# Vérification si l'auteur existe avant d'accéder à son nom #}
                        <p class="card-author">
                            {% if recipe.author is not null %}
                                <strong>Auteur: </strong>{{ recipe.author.name }}
                            {% else %}
                                <strong>Auteur: </strong>Auteur inconnu
                            {% endif %}
                        </p>

                        <p class="card-description"><strong>Description: </strong>{{ recipe.description }}</p>
                        <p class="card-difficulty"><strong>Difficulté: </strong>{{ recipe.difficulty }}</p>
                        <p class="card-steps"><strong>Étapes: </strong>
    {{ recipe.steps|join(', ') }}
</p>


                        {# Affichage des ingrédients #}
                        <p class="card-ingredients"><strong>Ingrédients: </strong>
                            {% for ingredient in recipe.getIngredients() %}
                                {{ ingredient }}{% if not loop.last %}, {% endif %}
                            {% endfor %}
                        </p>

                        {# Affichage de la catégorie #}
                        <p class="card-category"><strong>Catégorie: </strong>{{ recipe.category.name }}</p>

                       {% if app.user and recipe.author == app.user %}
                            <a href="{{ path('recipe_edit', {'id': recipe.id}) }}" class="btn btn-primary">Modifier</a>
                         {% endif %}

                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

<style>
body {
    background: 
             url('https://images.pexels.com/photos/1234567/pexels-photo-1234567.jpeg'); /* Dégradé de bleu clair vers bleu plus intense */
    font-family: 'Arial', sans-serif; /* Police pour un meilleur rendu */
    color: #333; /* Texte plus foncé pour un meilleur contraste */
    margin: 0;
    padding: 0;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: flex-start; /* Centrer tout le contenu verticalement */
}
.page-title {
    font-family: 'Poppins', sans-serif;
    font-weight: 600;
    font-size: 3rem;
    text-align: center;
    color:rgb(254, 253, 252); /* beige clair */
    text-shadow: 1px 1px 3px rgba(239, 231, 231, 0.2);
    margin-bottom: 40px;
    letter-spacing: 1.5px;
    user-select: none;
    opacity: 0;
    animation: fadeInTitle 1.2s ease forwards;
}

@keyframes fadeInTitle {
    to {
        opacity: 1;
    }
}

@media (max-width: 767px) {
    .page-title {
        font-size: 2.2rem;
        margin-bottom: 25px;
    }
}



.container {
    width: 80%;
    margin-top: 30px;
}

/* Titre de la page */
.page-title {
    font-size: 2.5rem; /* Grande taille de police pour le titre */
    font-weight: bold; /* Gras pour le titre */
    color: #343a40; /* Couleur sombre pour le titre */
    text-align: center; /* Centrer le titre */
    margin-bottom: 30px; /* Espacement sous le titre */
}

.recipe-card {
    background-color: #ffffff; /* Fond blanc pour les cartes */
    border-radius: 8px; /* Coins arrondis */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Légère ombre portée */
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    padding: 20px; /* Ajouter du padding pour plus d'espace à l'intérieur de la carte */
}

.recipe-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15); /* Ombre plus marquée au survol */
}
.recipe-card a.btn {
    display: inline-block;
    margin-top: 20px; /* espace au-dessus */
    padding: 8px 20px;
    border-radius: 25px;
    background: linear-gradient(45deg, #4a90e2, #1c60d1);
    color: white !important;
    font-weight: 600;
    font-size: 1rem;
    box-shadow: 0 3px 12px rgba(28, 96, 209, 0.6);
    text-transform: uppercase;
    transition: background 0.3s ease, box-shadow 0.3s ease, transform 0.2s ease;
    border: none;
    cursor: pointer;
    text-align: center;
}

.recipe-card a.btn:hover {
    background: linear-gradient(45deg, #1c60d1, #4a90e2);
    box-shadow: 0 5px 18px rgba(74, 144, 226, 0.8);
    transform: translateY(-2px);
    text-decoration: none;
    color: white !important;
}


.card-title {
    font-size: 1.5rem; /* Augmenter la taille de la police du titre */
    font-weight: bold; /* Bold title */
    color: #343a40; /* Couleur sombre pour le titre */
    margin-bottom: 15px; /* Espacement sous le titre */
}

.card-author {
    font-size: 1.1rem; /* Légèrement plus grand pour l'auteur */
    color: #6c757d; /* Couleur grise pour l'auteur */
    margin-bottom: 10px; /* Espacement sous l'auteur */
}

.card-description,
.card-difficulty,
.card-steps,
.card-ingredients,
.card-category {
    font-size: 1.1rem; /* Augmenter la taille de la police des sections */
    color: #495057; /* Couleur de texte gris clair */
    margin-bottom: 12px; /* Espacement entre les différentes sections */
}

.btn-primary {
    background-color: #007bff; /* Couleur bleue pour le bouton */
    border-color: #007bff; /* Bordure bleue pour le bouton */
    color: white; /* Texte en blanc */
    border-radius: 4px; /* Coins arrondis pour le bouton */
    padding: 12px 25px; /* Padding plus large pour un bouton plus grand */
    margin-top: 15px;
    text-transform: uppercase;
    font-weight: bold;
    transition: background-color 0.3s ease, border-color 0.3s ease;
}

.btn-primary:hover {
    background-color: #0056b3; /* Couleur plus sombre au survol */
    border-color: #0056b3;
}
.search-bar {
    display: flex;
    justify-content: flex-end;
    position: absolute;
    top: 25px; /* Remonte la barre un peu, proche du bouton Accueil */
    right: 20px;
    z-index: 1000;
    gap: 10px;
}

.search-bar input.form-control {
    width: 280px;
    padding: 10px 15px;
    border: none;
    border-radius: 30px;
    background-color: #f0f5ff; /* Bleu très clair */
    box-shadow: 0 2px 8px rgba(74, 144, 226, 0.2);
    font-size: 1rem;
    transition: box-shadow 0.3s ease;
    color: #343a40;
}

.search-bar input.form-control:focus {
    outline: none;
    box-shadow: 0 4px 12px rgba(28, 96, 209, 0.6);
    background-color: #ffffff;
}

.search-bar button.btn-primary {
    padding: 10px 25px;
    border-radius: 30px;
    background: linear-gradient(45deg, #4a90e2, #1c60d1);
    border: none;
    box-shadow: 0 4px 15px rgba(28, 96, 209, 0.6);
    font-weight: 700;
    text-transform: uppercase;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    letter-spacing: 1.2px;
    transition: background 0.4s ease, box-shadow 0.4s ease, transform 0.3s ease;
    color: white;
    cursor: pointer;
}

.search-bar button.btn-primary:hover {
    background: linear-gradient(45deg, #1c60d1, #4a90e2);
    box-shadow: 0 6px 20px rgba(74, 144, 226, 0.8);
    transform: translateY(-3px);
    color: white;
}

.home-button {
    position: absolute;
    top: 20px;
    left: 20px;
    z-index: 1000; /* Pour qu’il reste au-dessus */
}

.home-button .btn {
    font-weight: 700;
    text-transform: uppercase;
    padding: 12px 30px;
    border-radius: 30px;
    background: linear-gradient(45deg, #4a90e2, #1c60d1); /* dégradé bleu */
    color: #ffffff;
    border: none;
    box-shadow: 0 4px 15px rgba(28, 96, 209, 0.6);
    transition: background 0.4s ease, box-shadow 0.4s ease, transform 0.3s ease;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    letter-spacing: 1.2px;
    display: inline-block;
}

.home-button .btn:hover {
    background: linear-gradient(45deg, #1c60d1, #4a90e2);
    box-shadow: 0 6px 20px rgba(74, 144, 226, 0.8);
    transform: translateY(-3px);
    text-decoration: none;
    color: #ffffff;
}




/* Augmenter l'espace entre les cartes */
.row {
    display: flex;
    flex-wrap: wrap;
    gap: 30px; /* Espace entre les cartes */
}

/* Ajouter de l'espace en bas de chaque carte */
.col-md-3 {
    margin-bottom: 30px; /* Espace supplémentaire en bas de chaque carte */
}

/* Pour les petits écrans */
@media (max-width: 767px) {
    .col-md-3 {
        flex: 0 0 100%; /* Empiler les cartes sur les petits écrans */
        margin-bottom: 30px; /* Espace supplémentaire en bas de chaque carte */
    }
}
</style>
